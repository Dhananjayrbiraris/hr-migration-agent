# Optional. If unset, the app runs exactly as before with zero LLM calls --
# the mapping/cleaning/escalation logic is fully rule-based regardless.
# When set, escalation cards get an advisory "GPT-4o-mini suggests ..."
# note the consultant can accept with one click, correct, or ignore.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
